# Autogenerated by fastlane
#
# Ensure this file is checked in to source control!
# Gems install to fastlane/vendor/bundle (see .bundle/config).
#
# Setup (from repo root):
#   cd packages/fastlane_cli/fastlane && bundle install
#
# If you see "Could not find 'bundler' (= x.y.z)" or activate_bin_path errors, install
# a recent Bundler 2.x, then re-run bundle install:
#   gem install bundler
#   # If gem bin is not on PATH (system Ruby on macOS):
#   export PATH="$(ruby -e 'puts Gem.user_dir')/bin:$PATH"

source "https://rubygems.org"

gem 'fastlane'
gem 'cocoapods'
gem 'abbrev'
# Ruby 2.6 compatibility fix:
gem 'activesupport', '~> 5.2.0'

plugins_path = File.join(File.dirname(__FILE__), 'Pluginfile')
eval_gemfile(plugins_path) if File.exist?(plugins_path)
